<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sudipta Mandal | Developer Portfolio</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;600&family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    
    <style>
        /* --- 1. SETTINGS & RESET --- */
        :root {
            --bg-color: #0a0f1c; /* Darker Navy */
            --card-bg: #151e32;
            --text-main: #e2e8f0;
            --text-muted: #94a3b8;
            --accent: #00d4ff; /* Neon Blue */
            --accent-glow: rgba(0, 212, 255, 0.4);
            --code-font: 'Fira Code', monospace;
            --body-font: 'Poppins', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; scroll-behavior: smooth; }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            font-family: var(--body-font);
            overflow-x: hidden;
        }

        /* --- 2. ANIMATIONS & EFFECTS --- */
        /* Fade In Animation for scrolling */
        .hidden { opacity: 0; transform: translateY(30px); transition: all 1s ease; }
        .show { opacity: 1; transform: translateY(0); }

        /* Neon Glow Effect on Hover */
        .glow-on-hover:hover {
            box-shadow: 0 0 15px var(--accent-glow);
            border-color: var(--accent);
            transform: translateY(-5px);
        }

        /* --- 3. NAVIGATION --- */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 8%;
            position: fixed;
            width: 100%;
            background: rgba(10, 15, 28, 0.85);
            backdrop-filter: blur(12px);
            z-index: 1000;
            border-bottom: 1px solid rgba(255,255,255,0.05);
        }

        .logo { 
            font-size: 1.5rem; 
            font-weight: 700; 
            color: var(--accent); 
            font-family: var(--code-font);
        }
        
        .nav-links { display: flex; gap: 30px; }
        .nav-links a {
            color: var(--text-main);
            text-decoration: none;
            font-weight: 500;
            position: relative;
        }
        
        .nav-links a::after {
            content: '';
            width: 0; height: 2px;
            background: var(--accent);
            position: absolute;
            left: 0; bottom: -5px;
            transition: 0.3s;
        }
        .nav-links a:hover::after { width: 100%; }

        /* --- 4. HERO SECTION --- */
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: flex-start;
            padding: 0 15%;
            background: radial-gradient(circle at 80% 20%, rgba(0, 212, 255, 0.05) 0%, transparent 40%);
        }

        .hero h3 { color: var(--accent); font-family: var(--code-font); margin-bottom: 10px; }
        .hero h1 { font-size: 3.5rem; font-weight: 700; line-height: 1.1; margin-bottom: 15px; }
        .hero h2 { font-size: 2rem; color: var(--text-muted); margin-bottom: 20px; }
        
        /* Typing Effect Cursor */
        .typewriter span { color: var(--accent); border-right: 2px solid var(--accent); animation: blink 0.7s infinite; }
        @keyframes blink { 50% { border-color: transparent; } }

        .btn {
            margin-top: 30px;
            padding: 12px 28px;
            background: transparent;
            color: var(--accent);
            border: 1px solid var(--accent);
            border-radius: 4px;
            font-family: var(--code-font);
            cursor: pointer;
            text-decoration: none;
            transition: 0.3s;
        }
        .btn:hover { background: rgba(0, 212, 255, 0.1); }

        /* --- 5. SECTIONS --- */
        section { padding: 80px 10%; }
        .section-header { display: flex; align-items: center; margin-bottom: 40px; }
        .section-header h2 { font-size: 2rem; width: fit-content; margin-right: 20px; }
        .section-header .line { height: 1px; background: #334155; flex-grow: 1; max-width: 300px; }
        .section-header span { color: var(--accent); font-family: var(--code-font); font-size: 1.2rem; margin-right: 10px; }

        /* --- 6. GRIDS --- */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
        }

        /* Education & Stats Cards */
        .card {
            background: var(--card-bg);
            padding: 25px;
            border-radius: 8px;
            transition: 0.3s;
            border: 1px solid transparent;
        }
        .card h3 { font-size: 1.2rem; margin-bottom: 5px; }
        .card .sub { color: var(--accent); font-size: 0.9rem; font-family: var(--code-font); margin-bottom: 10px; display: block; }
        .card p { font-size: 0.95rem; color: var(--text-muted); }

        /* HackerRank Stats specific styling */
        .stat-list li { margin-bottom: 8px; color: var(--text-muted); display: flex; align-items: center; gap: 10px; }
        .stat-list i { color: gold; }

        /* Skills Styling */
        .skill-container { display: flex; flex-wrap: wrap; gap: 15px; }
        .skill-tag {
            background: #1e293b;
            color: var(--accent);
            padding: 8px 16px;
            border-radius: 4px;
            font-family: var(--code-font);
            font-size: 0.9rem;
            border: 1px solid transparent;
            transition: 0.3s;
        }
        .skill-tag:hover { border-color: var(--accent); background: rgba(0, 212, 255, 0.05); }

        /* Project Cards */
        .project-card {
            background: var(--card-bg);
            padding: 25px;
            border-radius: 8px;
            transition: 0.3s;
            position: relative;
            top: 0;
        }
        .project-top { display: flex; justify-content: space-between; align-items: center; margin-bottom: 15px; }
        .folder-icon { font-size: 2rem; color: var(--accent); }
        .github-icon { font-size: 1.2rem; color: var(--text-main); transition: 0.3s; }
        .github-icon:hover { color: var(--accent); }
        
        .project-card h3 { margin-bottom: 10px; }
        .project-card p { font-size: 0.9rem; color: var(--text-muted); margin-bottom: 20px; }
        .tech-stack { font-size: 0.8rem; font-family: var(--code-font); color: var(--text-muted); display: flex; gap: 10px; }

        /* Contact Section */
        .contact-box { text-align: center; max-width: 600px; margin: 0 auto; }
        .contact-links { display: flex; flex-direction: column; gap: 15px; margin-top: 30px; }
        .contact-item { 
            background: var(--card-bg); 
            padding: 15px; 
            border-radius: 5px; 
            display: flex; 
            justify-content: center; 
            align-items: center;
            gap: 10px;
            text-decoration: none;
            color: var(--text-muted);
            transition: 0.3s;
        }
        .contact-item:hover { color: var(--accent); background: #1e293b; }

        /* Footer */
        footer { text-align: center; padding: 20px; font-size: 0.8rem; font-family: var(--code-font); color: var(--text-muted); }

        @media (max-width: 768px) {
            .hero { padding: 0 8%; align-items: center; text-align: center; }
            .hero h1 { font-size: 2.5rem; }
            .nav-links { display: none; }
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">&lt;Sudipta /&gt;</div>
        <ul class="nav-links">
            <li><a href="#about"><span style="color:var(--accent)">01.</span> About</a></li>
            <li><a href="#skills"><span style="color:var(--accent)">02.</span> Skills</a></li>
            <li><a href="#projects"><span style="color:var(--accent)">03.</span> Projects</a></li>
            <li><a href="#contact"><span style="color:var(--accent)">04.</span> Contact</a></li>
        </ul>
    </nav>

    <section class="hero">
        <h3 class="hidden">Hi, my name is</h3>
        <h1 class="hidden">Sudipta Mandal.</h1>
        <h2 class="typewriter hidden">I build <span id="typing-text"></span></h2>
        <p class="hidden" style="max-width: 500px; color: var(--text-muted); margin-top: 20px;">
            I am a 1st Year B.Tech CSE student at IILM University. I focus on logic building, data structures, and creating clean software solutions.
        </p>
        <a href="#contact" class="btn hidden">Get In Touch</a>
    </section>

    <section id="about">
        <div class="section-header hidden">
            <span>01.</span> <h2>Education & Stats</h2> <div class="line"></div>
        </div>
        <div class="grid">
            <div class="card glow-on-hover hidden">
                <h3>B.Tech CSE</h3>
                <span class="sub">IILM University, Greater Noida</span>
                <p>Current 1st Year Student</p>
                <p style="margin-top:10px; font-size:0.85rem">Relevant Coursework: C Programming, Math, Physics.</p>
            </div>
            
            <div class="card glow-on-hover hidden">
                <h3>Schooling</h3>
                <span class="sub">NIOS & St. Mary's Malda</span>
                <p><strong>12th Grade:</strong> 92.25% (NIOS)</p>
                <p><strong>10th Grade:</strong> 76.2% (St. Mary's)</p>
            </div>

            <div class="card glow-on-hover hidden" style="border-left: 3px solid var(--accent);">
                <h3><i class="fa-solid fa-code"></i> HackerRank Stats</h3>
                <ul class="stat-list" style="margin-top: 15px;">
                    <li><i class="fa-solid fa-star"></i> 3 Star in Java</li>
                    <li><i class="fa-solid fa-star"></i> 3 Star in C</li>
                    <li><i class="fa-solid fa-star-half-stroke"></i> 1 Star Problem Solving</li>
                </ul>
            </div>
        </div>
    </section>

    <section id="skills">
        <div class="section-header hidden">
            <span>02.</span> <h2>My Tech Stack</h2> <div class="line"></div>
        </div>
        <div class="skill-container hidden">
            <div class="skill-tag"><i class="fa-brands fa-java"></i> Java</div>
            <div class="skill-tag">C Language</div>
            <div class="skill-tag">Data Structures</div>
            <div class="skill-tag"><i class="fa-brands fa-html5"></i> HTML5</div>
            <div class="skill-tag"><i class="fa-brands fa-css3-alt"></i> CSS3</div>
            <div class="skill-tag">Problem Solving</div>
            <div class="skill-tag">Git & GitHub</div>
            <div class="skill-tag">VS Code</div>
        </div>
    </section>

    <section id="projects">
        <div class="section-header hidden">
            <span>03.</span> <h2>Projects</h2> <div class="line"></div>
        </div>
        <div class="grid">
            
            <div class="project-card glow-on-hover hidden">
                <div class="project-top">
                    <i class="fa-regular fa-folder folder-icon"></i>
                    <a href="#" class="github-icon"><i class="fa-brands fa-github"></i></a>
                </div>
                <h3>Bank Management System</h3>
                <p>A console-based application handling generic banking transactions like deposits, withdrawals, and account creation using file handling.</p>
                <div class="tech-stack">
                    <span>C Language</span>
                    <span>File I/O</span>
                </div>
            </div>

            <div class="project-card glow-on-hover hidden">
                <div class="project-top">
                    <i class="fa-regular fa-folder folder-icon"></i>
                    <a href="#" class="github-icon"><i class="fa-brands fa-github"></i></a>
                </div>
                <h3>Portfolio Website</h3>
                <p>This responsive dark-themed portfolio website. Features scroll animations, typewriter effects, and a mobile-friendly layout.</p>
                <div class="tech-stack">
                    <span>HTML</span>
                    <span>CSS</span>
                    <span>JavaScript</span>
                </div>
            </div>

            <div class="project-card glow-on-hover hidden">
                <div class="project-top">
                    <i class="fa-regular fa-folder folder-icon"></i>
                    <a href="#" class="github-icon"><i class="fa-brands fa-github"></i></a>
                </div>
                <h3>Logic Patterns</h3>
                <p>A collection of complex star patterns (Diamond, Pyramid) algorithms solved to demonstrate loop logic mastery.</p>
                <div class="tech-stack">
                    <span>Java</span>
                    <span>Logic Building</span>
                </div>
            </div>

        </div>
    </section>

    <section id="contact">
        <div class="contact-box hidden">
            <p style="color: var(--accent); font-family: var(--code-font);">04. What's Next?</p>
            <h2 style="font-size: 2.5rem; margin: 10px 0;">Get In Touch</h2>
            <p style="color: var(--text-muted); margin-bottom: 30px;">
                I am currently looking for new opportunities and my inbox is always open. Whether you have a question or just want to say hi, I'll try my best to get back to you!
            </p>

            <div class="contact-links">
                <a href="mailto:sudipta.25scs1003003500@iilm.edu" class="contact-item">
                    <i class="fa-solid fa-envelope"></i> College Email
                </a>
                <a href="mailto:mandalsudipta2006@gmail.com" class="contact-item">
                    <i class="fa-solid fa-envelope"></i> Personal Email
                </a>
                <a href="https://www.linkedin.com/in/sudipta-mandal-8aa14a309/" target="_blank" class="contact-item">
                    <i class="fa-brands fa-linkedin"></i> LinkedIn
                </a>
                <a href="tel:9339190621" class="contact-item">
                    <i class="fa-solid fa-phone"></i> +91 93391 90621
                </a>
            </div>
        </div>
    </section>

    <footer>
        <p>Built by Sudipta Mandal | &copy; 2025</p>
    </footer>

    <script>
        // 1. SCROLL REVEAL ANIMATION
        const observer = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('show');
                }
            });
        });

        const hiddenElements = document.querySelectorAll('.hidden');
        hiddenElements.forEach((el) => observer.observe(el));

        // 2. TYPEWRITER EFFECT
        const textElement = document.getElementById('typing-text');
        const texts = ["algorithms in C.", "Java applications.", "logic for problems."];
        let count = 0;
        let index = 0;
        let currentText = "";
        let letter = "";

        (function type() {
            if (count === texts.length) {
                count = 0;
            }
            currentText = texts[count];
            letter = currentText.slice(0, ++index);

            textElement.textContent = letter;
            if (letter.length === currentText.length) {
                count++;
                index = 0;
                setTimeout(type, 2000); // Wait 2s before typing next
            } else {
                setTimeout(type, 100); // Typing speed
            }
        })();
    </script>
</body>
</html>
